import { ActivityView } from "../activity/ActivityView.js";

/**
 * UnifiedLogsPage now presents the Actionable Activity view (P4),
 * retaining access to Librarian History (with Undo), Curator Logs,
 * and System Console under the Diagnostics & History tab.
 */
export const UnifiedLogsPage = ActivityView;
